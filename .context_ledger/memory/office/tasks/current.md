# Current Task (overwrite each session)

Holds exactly one task — the one being worked on right now. Set it at
session start (protocol Step 3), clear it at session end (Step 15). If
you find a stale in-progress entry here, a prior session died mid-task —
its roster row (if left behind) says who was here; check the session
entry and backlog before starting.

<!-- TEMPLATE — replace everything below this comment:
- **Session:** YYYY-MM-DD — <agent> / <model>
- **Task:** <what is being worked on right now>
- **Status:** blocked (push not permitted from this session; target platform unknown — P-2026-09-24-1) | done | blocked (<blocker>)
-->

- **Session:** 2026-09-24 — Amara / claude-sonnet-5
- **Task:** Layers 1-2 proven on both platforms; layer 3 built and live-verified on Deriv. Now systematically testing real YouTube-claimed digit strategies against the replay harness.
  - **Layer 1** (recorder): CryptonicHub (7,585 real ticks, feed clean) and Deriv (`wss://api.derivws.com/trading/v1/options/ws/public` — the commonly-documented `ws.derivws.com/v3` is retired, see 6f171d5) both proven live. A `record-deriv --ticks 8000` recording is in its final stretch (~6,691/8,000) for a same cross-platform check+replay comparison as CryptonicHub — not yet run.
  - **Layer 3** (executor, Deriv only): `clicktrader/api/deriv/trading.py` (8d184bb) implements the documented OTP flow (`POST .../accounts/{accountId}/otp` → private WS URL → `proposal`/`buy`), defaults to refusing a `/ws/real` URL. Live-verified with a real $0.35 demo contract (8bc36a5, user's own terminal — Claude Code's permission classifier correctly refuses to place trades, demo or not, from this session; not a bug). Setup facts worth knowing (actual IDs are in the user's local `.env`, never this public repo — the classifier caught one attempt to commit them and was right to): the shared public `app_id` doesn't work for account-scoped calls, an application must be registered separately from the token page; account IDs must come from `GET /trading/v1/options/accounts`, never guessed; Deriv's minimum stake for `DIGITOVER`/`1HZ10V`/1-tick is $0.35, above the strategies' $0.10 backtest default.
  - **Strategy testing** (838a5c4): eight registered claims now — `low-digit-over`, plus four newly formalized from actual videos (`cold-tail-over-{2,3}`, `cold-tail-under-{6,7}`, `cold-tail-reactive-over-2`, `parity-counter-trend-{threshold,majority}`, `martingale-low-digit-over`). All eight converge to "No edge" on synthetic data — one showed "worse than pricing" on a single seed, confirmed as multiple-comparisons noise on 3 more seeds, not a real effect. `model.py` gained EVEN/ODD as a fixed-50/50 contract type (no barrier) for the parity strategies. `MartingaleOnLoss` is a generic stake-only wrapper usable on any strategy; its real finding is drawdown ($1,686 vs ~$95-190 for fixed-stake), not EV (same -5%) — the measured version of "50/50 but a good strategy profits," which is false for entry timing and true only in the sense that *staking* changes risk, not odds. One video (real-futures order-flow scalping, Auction Market Theory) was correctly identified as inapplicable — synthetic indices have no real volume/order flow to analyze.
  Deriv recording finished (8,000 ticks): feed clean (uniformity p=0.86; lag-1 independence p=0.045 — clears α=0.01 but notably closer to the line than CryptonicHub's 0.37, worth watching on a future recording rather than ignoring), `low-digit-over` again "No edge" (-1.8% vs pricing's -5.0%) — same conclusion as CryptonicHub, two platforms independently agreeing.

  **Live-runner built** (a8d6b4b): `clicktrader/executor.py` + `clicktrader run-deriv` ties layers 1-3 together — watches real ticks, checks `RiskGuard` before every trade (no bypass), places it, grades the outcome against the next tick itself reads (a documented simplification: not verified against Deriv's own `proposal_open_contract` settlement message, worth strengthening later), writes the same `LedgerRow` shape replay does. `--max-stake`/`--max-session-loss`/`--max-consecutive-losses` are all required with no defaults — deliberate, limits are the feature. Demo-only, no override flag. Not run live yet — same reason the single test trade wasn't: Claude Code's permission classifier correctly refuses real-world financial actions from this session regardless of demo gating; needs the user's own terminal, same pattern as before.
- **Status:** open — no blockers. Loose ends: (1) the user running `run-deriv` themselves for the first live smoke test (suggested first-run limits: small — e.g. `--max-stake 1 --max-session-loss 5 --max-consecutive-losses 5`); (2) strengthen executor.py to verify settlement via `proposal_open_contract` instead of self-grading, before trusting it beyond demo learning; (3) DESIGN.md doesn't yet mention the -5% EV algebraic proof, the Deriv account setup process, or the growing strategy registry — worth a documentation pass (`B-2026-09-24-6`); (4) `B-2026-09-24-3` (multiple-comparisons correction on the harness's verdict) got a concrete, live illustration of why it matters (one of 8 strategies looked "significant" on one seed, purely by chance) — worth bumping priority.
