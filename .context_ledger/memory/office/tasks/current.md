# Current Task (overwrite each session)

Holds exactly one task — the one being worked on right now. Set it at
session start (protocol Step 3), clear it at session end (Step 15). If
you find a stale in-progress entry here, a prior session died mid-task —
its roster row (if left behind) says who was here; check the session
entry and backlog before starting.

<!-- TEMPLATE — replace everything below this comment:
- **Session:** YYYY-MM-DD — <agent> / <model>
- **Task:** <what is being worked on right now>
- **Status:** blocked (push not permitted from this session; target platform unknown — P-2026-09-24-1) | done | blocked (<blocker>)
-->

- **Session:** 2026-09-24 — Amara / claude-sonnet-5
- **Task:** CryptonicHub layer 1 proved out end-to-end: 7,585 real ticks recorded (`recordings/live-cryptonichub.jsonl`), feed checks clean (chi2 uniformity p=0.66, lag-1 independence p=0.37), `low-digit-over` (user's Over(1)-after-0/1 claim) replayed with no evidence of an edge — consistent with the pricing math being provably -5% EV regardless of barrier (p cancels out algebraically in `Contract.profit_ratio`'s derivation; see chat, not yet written into DESIGN.md). Recorder hardened against a real page-navigation crash (174d5bd, 748062a). User then pivoted primary interest to Deriv (real documented API, demo account, no edge-hunting expected there either — it's the same synthetic-index product line, just better engineering + a licensed counterparty). Built `clicktrader/api/deriv.py` (d340856): public WebSocket ticks, no auth needed for layer 1, a Trade-scoped demo API token already generated by the user and stored in `.env` (untracked, not needed until layer 3).
- **Status:** blocked (external) — Deriv's WebSocket backend (`ws.derivws.com` and every documented mirror) has been returning Cloudflare 520s since before the adapter was written; confirmed via curl, Python, and a real browser from deriv.com's own origin, so it's a Deriv-side outage, not anything blocking us specifically. The adapter's retry/backoff and give-up path were confirmed live against the outage itself (behaves correctly); the actual tick-parsing success path is unverified pending recovery. Next step the moment it's back: `clicktrader record-deriv recordings/live-deriv.jsonl --symbol 1HZ10V`, then the same `check` + `replay --strategy low-digit-over` pipeline already proven on CryptonicHub, for a cross-platform comparison. Retest the WS endpoint before assuming it's still down — it may recover without a code change needed.
